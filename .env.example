OPENAI_API_KEY=your_api_key_here

# Optional: override the default model (defaults to gpt-4o)
# OPENAI_MODEL=gpt-4o

# Optional: kubeconfig path (defaults to ~/.kube/config)
# KUBECONFIG=/path/to/kubeconfig